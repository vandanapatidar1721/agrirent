/**
 * Copy to config.js and set your API URL before deploying the frontend.
 * config.js is safe to commit if it only contains public URLs (no secrets).
 */
window.APP_CONFIG = {
  API_BASE_URL: "http://localhost:5000",
};
